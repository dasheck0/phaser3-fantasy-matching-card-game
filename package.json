{
  "name": "dasheck0-phaser-memory",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "build:production": "vite build --mode production",
    "release": "git checkout master && git merge develop && git push origin master --tags && git checkout develop && git push origin develop",
    "release:patch": "npm version patch && npm run release",
    "release:minor": "npm version minor && npm run release",
    "release:major": "npm version major && npm run release"
  },
  "author": "Stefan Neidig <s.neidig@nanogiants.de> (https://appcom-interactive.de/)",
  "license": "MIT",
  "dependencies": {
    "@dasheck0/phaser-boilerplate": "^1.2.3",
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.6",
    "typescript": "^5.2.2",
    "vite": "^4.4.9"
  }
}
